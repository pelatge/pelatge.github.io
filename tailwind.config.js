module.exports = {
  content: [
  'index.html',
  ],
  darkMode: 'class',
  theme: {
    container: {
      center: true,
      padding: '16px',
    },
    extend: {
      },
    },
  plugins: [
   // include Flowbite as a plugin in your Tailwind CSS project
   // require('flowbite/plugin')
  ],
};
